matrixgenerator

generate all nxm binary matrices

to compile, simply type 'make' in this directory.  This will build an
executable 'matrix_generator' in this directory.  To output all nxm
matrices to stdout, simply type

./matrix_generator n m
